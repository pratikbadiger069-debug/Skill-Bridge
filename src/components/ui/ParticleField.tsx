'use client';

import React from 'react';

export interface ParticleFieldProps {
  count?: number;
  className?: string;
}

/**
 * ParticleField component disabled/cleaned up for ZERO × SkillBridge minimal design system.
 * Minimal design system strictly avoids particle animations and ambient neon shapes.
 */
export function ParticleField({ count, className }: ParticleFieldProps = {}) {
  return null;
}

export default ParticleField;
